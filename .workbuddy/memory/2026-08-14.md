# 2026-08-14 工作记录（deepthinkSingle 看盘项目）

## 历史分时白图修复（缺陷#14）验证
- 修复代码早已落地（`static/js/app.js` showHistMinute/renderHistChart：弹窗销毁重建 + reflow + resize + try/catch）。
- 本次补 jsdom 真·回归测试（双击 K 线 → 弹窗 → /api/minute → setOption 正确渲染 48 点；空数据边界），`node --test tests/js/dom-smoke.test.js` 12/12 全过。

## 历史分钟 K 全量（通达信 .lc1 本地化）
- 用户理解正确：60分K 应是跨交易日完整序列（约每天4根），之前只有4根是 npx 当日切片。
- 实现：`sources/tdx.py` 新增 `read_tdx_minute()`（读 .lc1，32字节小端 `<HHfffffII>`）+ `aggregate_minute()`（按交易日分组聚合 m1/m5/m15/m30/m60）；`TdxSource.get_kline` 支持分钟周期；`get_minute` 历史 date 走本地 .lc1。
- `config.KLINE_MIN_SOURCES = ["tdx","npx"]`；`quote_service.get_kline` 分钟 K 缓存改存全量历史（下限剔除旧 npx 当日4根切片）；`get_minute` 历史 date 链 `["tdx"]+QUOTE_SOURCES`，突破腾讯30天限制。
- 前端 `loadKline` 分钟 limit=2000；`showHistMinute` 取 date 日期部分（tdx 分钟 date 含时间 "2026-08-03 10:30"，否则 compact 带空格传给 /api/minute 出错）。
- **关键坑**：.lc1 的 date/time 是 uint16，2026+ 编码值 >32767，必须用无符号 `H` 解析，否则溢出变负。
- 测试 `tests/test_tdx_minute.py` 6 项（构造 .lc1 fixture 验证聚合条数/OHLC/历史分时过滤）；沙盒无 D: 盘，真实 .lc1 需本机验证。
- 文档同步：architecture.md / testing.md / backlog.md（v0.0.5）。

## 环境注意
- 本机 Python313 含 flask/requests；沙盒 `python` 缺依赖（装 requests 后后端仍有股票列表抓取类失败，与本次无关）。
- backend 测试失败项中 test_api 加载错误/test_stock_list 需联网，均非本次改动引入。
- 项目根有上次 CDP 截图验证遗留的 tmp_* 文件（tmp_cdp_screenshot.py / tmp_hist_* / tmp_render_svg.js），待用户确认是否清理。

## 2026-08-15 修正：分钟 K 全量 + 真实 D 盘验证（重要）
- **沙盒可读 D 盘！** 之前误判"沙盒读不到 D:/new_tdx64/vipdoc"是错的（日 K 线当时就能读 .day）。验证 tdx 必须直接用真实 `.lc1`，路径用 `/d/new_tdx64/vipdoc/sh/minline/sh600519.lc1`（Git Bash 下 `/d/...`）。
- **缺陷#16：分钟 K 全量被后端 2000 上限截断**。`services/quote_service.py` 原 `fetch_limit = 0 if day else max(limit or 260, 2000)`，tdx 返回全量后被 `rows[-2000:]` 截掉，m1/超长 m5 丢数据；前端 `app.js:1009` 也只请求 2000、标题错标「当日」。已改为 `fetch_limit = 0` + 前端 `limit = 0` + 标题「本地」。
- **真实数据验证结果**（sh600519）：解析 7680→10320 根全对；聚合 m60=每交易日4根/m30=8/m15=16/m5=48 精确吻合；历史分时任意交易日=240 点。端到端 `get_kline` m1=10320/m60=172 全量跨交易日。
- **教训：fixture 自洽测不出截断/解码 bug**。日期解码用 `divmod(d,2048)`（正确，非位运算）；任何 tdx 改动都需用真实 `.lc1` 跑一遍数量/OHLC/聚合结构校验。
- `test_tdx_minute.py` 扩至 8 项（新增 `QuoteServiceFullHistoryTest`：2640 根 fixture 经 quote_service 验证返回全量非截断）。
- 文档已同步 testing.md（基线后端134/前端17、缺陷#16）、backlog.md（v0.0.5 追加行）。

## 2026-08-15 追加：宁德时代分钟 K "数据为空" + UX 修复
- 用户报 `sz300750` 30 分 K 线显示"K线加载失败: K线数据为空"，且画面残留茅台旧 K 线。
- 排查：后端 `/api/kline?code=sz300750&period=m30&limit=0` 实际返回 504 根正常数据；5000 端口服务正常。截图大概率是旧服务进程/旧浏览器缓存导致。
- 但暴露两个真实 UX 缺陷：
  1. `/api/kline` 500 时返回 `{error:...}`，前端 `r.json()` 解析成对象而非数组，`lastErr` 为空，最终错显"数据为空"。
  2. K 线加载失败不清 charts.p4，上一只股票图表残留。
- 修复：`loadKline` 检查 `data.error` 并记入 `lastErr`；失败时 `charts.p4.clear()` + 置空 option；`app.js?v=35` → `v=36` 强制浏览器刷新。前端 jsdom 12/12 仍过。
- 教训：前端错误处理要区分"后端返回 500 JSON"与"空数组"；K 线失败必须清图。

## 2026-08-15 补充：run.bat 启动体验改进
- 原 run.bat 在启动服务前就开浏览器，导致先弹无法连接。改为：独立窗口后台启动服务 + curl /api/sysinfo 轮询（最多30s）就绪后开浏览器。
- 用户习惯用 run.bat 启动。排查旧版本/熔断：优先确认浏览器 Ctrl+F5 硬刷新 + 服务是否真用新代码重启用 curl 5000 端口 /api/kline 看返回数组还是 npx 熔断 JSON。
- 已 curl 验证：当前 5000 端口 python=3.13.3、海尔智家 sh600690 m5 返回 3024 根数组（tdx 优先生效），服务端为新代码。

## 2026-08-15 终局：分钟K陈旧缓存(缺陷#18) + 真实浏览器E2E
- 用户反复报"换股票分钟K不行"——根因不在代码逻辑，而是**运行进程脏了**：旧代码进程/熔断/磁盘缓存。干净重启后工商银行等立即正常(504根)。
- **缺陷#18（真 bug）**：quote_service.get_kline 的"通达信本地有更新则重拉"过期检查只对 ptype=="day" 生效，分钟K永不检查；kline_cache 是 FileCache 落盘(TTL 24h)，旧调试期写的茅台 m30 陈旧值(止于07-06)重启进程也清不掉→一直命中旧值，而保利/工商/宁德是后来才写入的新鲜值(08-03)，表现成"茅台陈旧、其他新鲜"。
- 修复：过期检查扩展到 (ptype=="day" or ptype=="min")；手动清空 data/kline_cache 陈旧文件；test_tdx_minute.py 新增 test_minute_cache_staleness_refresh (mock tdx_last_date=未来 强制过期, 守命中全量88而非陈旧4)。
- **新增真实浏览器 E2E**：tests/e2e_kline.js 用 puppeteer-core(系统Chrome,不下载Chromium)打开真实页面→切茅台/保利/工商/宁德→30分K→断言 #ch4 图表数据点。运行需先起服务 + npm i puppeteer-core。靠它在真实环境发现 #18。
- 验证：E2E 4只股全 PASS(均504根·截至08-03·本地)；test_tdx_minute 9/9；dom-smoke 12/12；py_compile OK。清理6个早期 tmp_* 调试文件。
- **核心教训**：本项目排查"为什么还是旧版本/熔断"必须 (1) kill 掉 5000 旧进程干净重启 (2) 浏览器 Ctrl+F5 硬刷 (3) 用真实浏览器+E2E 验证，不能只靠 curl/单测，因为磁盘缓存与陈旧进程会掩盖问题。
