# UI 审查报告（2026-08-13）

> 审查对象：templates/index.html + static/js/app.js + static/css/style.css
> 对照：docs/images/ 参考图（同花顺/东财通用样式）

## 1. 发现并修复的 UI Bug

| # | 严重度 | 问题 | 根因 | 修复 |
|---|---|---|---|---|
| 1 | 🔴 致命 | 整个前端 JS 无法执行 | `app.js:147` `$("klineTitle").textContent = "加载中…");` 多余 `)` → 脚本解析失败，三面板全空 | 删除多余括号（此 bug 很可能是之前"三面板静默不画"的真凶之一） |
| 2 | 🔴 致命 | 点「+」报错，加自选功能失效 | `addWatch()` 引用不存在的 `#addInput` 元素（HTML 无此输入框，仅 CSS 残留样式） | 改为「+」= 把**当前标的**加入自选（盯盘场景最常用），按钮加 title 提示 |
| 3 | 🟠 高 | 快速切换标的/周期时旧请求覆盖新数据（显示错标的） | `loadQuote` / `loadKline` 无竞态保护 | 加 `_quoteSeq` / `_klineSeq` 序号，过期响应丢弃 |
| 4 | 🟢 低 | 顶部代码显示纯代码（600519），与自选/搜索的完整代码（SH600519）不一致 | `renderHeader` 用 `q.code`（已 pure） | 改显示完整代码 `fullCode.toUpperCase()` |
| 5 | 🟢 低 | `#addInput` CSS 残留 | 交互改版后未清理 | 删除 |

## 2. 与参考图对齐情况

| 参考图特征 | 现状 | 差距 |
|---|---|---|
| 黑底三面板（分时/VOLFS/主力） | ✅ 已实现 | — |
| 分时主图：白线现价 + 黄线均价 | ✅ 已实现 | — |
| 分时**右轴涨跌幅%** | ✅ 本次补齐（蓝色虚线） | 之前缺失 |
| VOLFS 红涨绿跌着色 | ✅ 已实现（按主力分钟增量） | — |
| 主力追踪紫色面积图（亿刻度） | ✅ 已实现 | — |
| 股票搜索（代码/名称/行业）+ 下拉 | ✅ 已实现 | 缺键盘↑↓导航 |
| 三级导航（列表→分时→K线） | ⏳ 列表页待 Sprint 3 | — |
| K线：蜡烛 + MA + VOL + 周期切换 | ✅ 已实现 | — |

## 3. 未修项（记 backlog，避免过度工程）

1. **搜索下拉键盘导航**（↑↓ 选择 + Enter 确认）——当前仅 Enter 选第一条；
2. **加载/错误可视化**：图表 loading 已有（`showLoading`），但三面板无数据时仅有空图——可加"暂无分时数据"占位文案；
3. **移动端适配**：`.wrap` 已设 max-width，小屏下顶部栏 flex-wrap 已能换行，但图表高度固定——后续按视口高度自适应；
4. **自选删除**：下拉选中重复标的可点「-」——当前仅能通过 API 删；
5. **K线 m5/m15 等分钟周期自动刷新**：当前仅分时视图 30s 刷新。


## 3.5 本轮功能增强（19:59）

| 项 | 修复 |
|---|---|
| K线标题防错配 | `switchTo` 清 latestData + 标题改用 `current.toUpperCase()` |
| 下拉菜单中文说明 | `/api/watchlist` 返回 `[{code, name}]`，search_service 加 `get_code_name()`，下拉显示 `五粮液 (SZ000858)` |
| 五粮液日K 全量 | `fetch_limit=0` 全量入缓存 + 清旧 260 缓存 → 6722 条 |
| 副图紫色量柱 | `renderFundGame` 改同花顺配色：主力净额紫色柱+散户绿色线+零轴虚线（对齐"主力提前买入→拉升"识别） |
| K线 tooltip 中文 | `renderKline` 加 `formatter` 函数，K线显示"开盘/收盘/最低/最高"、MA/成交量中文标签 |
| 分钟 K fallback | `loadKline` 数据 <10 根时自动 m60→m30→m15→m5→day fallback，并高亮实际显示周期按钮 |
| 搜索纯数字 | `search_stocks_with_guess`：纯数字按首位（0/3→sz, 6/9/5→sh, 4/8→bj）补前缀，返回占位项让用户试探 |
| 搜索占位项带真实名 | `search_stocks_with_guess` 增加 `_probe_name()`：同步探测腾讯 qt.gtimg.cn 拿真实中文名（1.5s 超时），失败降级"未确认" |
| 分时均价线醒目 | `renderMinute` 均价线改黄色点线（#ffd700，width 1.8，dashed），黑底高对比 |
| K线信息卡增强 | tooltip formatter 加昨收/涨幅（红绿色）/振幅/成交额（估算 亿元），K线 hover 时信息丰富 |
| 五档盘口信息卡 | `tencent.get_quote` 扩展解 f[9..28] 五档买卖 + f[33/34] 高低 + f[38] 换手率 + f[30] 时间；前端分时图右侧浮窗显示五档盘口 + 开高低/换手振幅/时间统计 |
| K线双击/回车 → 历史分时小图 | 后端 `/api/minute?date=YYYYMMDD`（腾讯 ifzq minute/query 支持历史日），K线面板 dblclick/Enter 弹模态显示该日分时（最近约 30 天免费源） |
| K线回车键 | 在 K 线视图下全局 Enter 监听 → 弹历史分时（与双击同效） |


## 4. 结论

前端在本次审查后达到可稳定交付状态：致命语法错误 + 交互失效已修复，竞态保护增强，分时右轴对齐参考图。剩余项为增强型 backlog，不影响主流程。

## 5. 功能增强（2026-08-13 19:51）

| 项 | 说明 |
|---|---|
| ④ 资金博弈副图（新增第 4 个分时面板） | 适配通达信「分时资金博弈 - Level2精简版」公式：主力净额=(超B+大B)-(超S+大S) 红线、散户净额=小B-小S 绿线、零轴灰虚线、条件柱（主力>0且散户<0 红柱 / 主力<0且散户>0 绿柱）。数据取东财分钟资金流累计值的**每分钟增量**，单位万元 |
| K线全量显示 | 日/周/月不再限制 260 根，本地通达信有多少显示多少（日K 5978 根 / 周K 1196 根），dataZoom 缩放浏览；分钟 K 仍取最近 260 根 |

> Level2 适配说明：通达信 L2_AMO 需 Level2 数据源；本项目用东财公开分钟资金流（大单/超大单/小单净额）近似实现同语义指标。

## 6. UI 增强（2026-08-13 22:25 ~ 23:39）

| 项 | 说明 |
|---|---|
| 分时均价 100 倍单位修复 | 腾讯 ifzq 分时 vol 单位"手"，`cum_amt/(cum_vol*100)` 修正，Y 轴恢复合理范围 |
| 收盘后伪数据截断 | 腾讯收盘后持续返回"最后一帧"，按价格最后变动位置截断（去掉 15:00 后 25 根） |
| VOLFS 累计改差分 | 每分钟成交量 = vol[i]-vol[i-1]（原显示累计总量单调上升） |
| VOLFS 染色对齐价格涨跌 | 红涨绿跌（A 股惯例），原按主力资金流方向 |
| 五档盘口移出分时图 | `.p1-wrap` 绝对定位浮窗 → 独立右侧栏（不遮挡图表） |
| 分钟详情独立面板 | `#minuteDetail` 与五档盘口上下排布，右侧栏 flex column |
| 方向键切换分钟 | document 级 keydown（焦点非 INPUT 时）↑/↓ 切换选中分钟 |
| 240 行滚动列表 | 每行 时间/价格(涨跌染色+箭头)/量(手)/额(万)，蓝底高亮选中行，自动滚动居中 |
| 列表与资金博弈图底框等高 | ResizeObserver 同步 `.minute-left` 高度给 `.minute-right` |
| 五档盘口美化 | 分组布局：标题涨跌色块 / 卖5→买5 / 现价大字(18px) / 16 项指标 2 组/行 / ⏱ 时间戳 |
| 时间戳格式化 | `fmtSnapshotTime(20260813161452 → 16:14:52)` |
| 指标补全 16 项 | 现价/今开/涨跌/最高/涨幅/最低/总手/量比/外盘/内盘/换手/股本/净资/流通/收益/PE（腾讯 f[7]/f[8]/f[39]/f[44]/f[45]/f[46]/f[49]/f[51]） |
| 自选下拉输入式搜索 | `<input list=datalist>` 5549+ 项（全 A 股 + 拼音），HTML5 模糊搜索 |
| 列表微调 | 时间加冒号（09:30）、滚动条 8px 细条、表头与内容列对齐、表头"量(手)"语义清晰 |
| 综合数据页 11 组 | 左侧栏 #p6 重构：行情/估值/财务/年度净利柱状图/股东/两融/龙虎榜/公司/盈利预测/主力多空/公告（数据源失败该组隐藏，独立 try/except） |
| 三列布局 | 对齐示例图：左列图表组（分时/VOLFS/主力/资金博弈）flex:1 + 中列（五档盘口+行情/估值/财务/净利柱/多空/两融）250px + 右列（分钟详情列表+股东/龙虎榜/公司/预测/公告）210px，中右列独立滚动 |
| 右键菜单（Sprint 2 追加） | 任意区域右键：⚙ 副图配置 / 📊 K线副图配置（K线视图）/ 💧 资金流明细 / 📥 导出 CSV / 🔄 立即刷新 |
| 副图配置（Sprint 2 追加） | 分时 13 个副图可选（volfs/mainflow/game/smallnet/bignet/superbig/middlenet/power/turnover/outerbuy/amtdiff/mainratio/day5），1-5 个窗口，localStorage 持久化 |
| K线技术指标副图（Sprint 2 追加） | MACD/KDJ/BOLL/RSI 4 个可选，主图 dataZoom 同步缩放 |
| 自选批量表格（Sprint 3） | 顶部"自选"按钮 → 表格视图（代码/名称/现价/涨跌幅/涨跌额/成交额/换手），点击行切换分时 |
| 资金流明细（Sprint 3） | 右键"资金流明细"modal：240 行 × 主力/超大/大/中/小单（万元，红进绿出） |
| 近5日主力（Sprint 4） | 副图类型 day5：5 日主力净流入柱状图（东财 f178，带数值标签） |
| 主力异动告警（Sprint 4） | 资金博弈图单分钟主力净额 `|差分|>中位数×3` → 橙色柱体高亮 |
| CSV 导出（Sprint 4） | 右键"导出 CSV"：分时 + 资金流两段，Blob 下载 |
